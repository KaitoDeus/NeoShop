name: CI/CD Pipeline

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  backend-build-test:
    name: Backend Build & Test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: "21"
          distribution: "temurin"
          cache: maven

      - name: Ensure Maven Wrapper is executable
        run: chmod +x backend/mvnw

      - name: Build with Maven
        run: cd backend && ./mvnw -B package --file pom.xml

  frontend-build:
    name: Frontend Build Check
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Install Dependencies
        run: cd frontend && npm ci

      - name: Build Project
        run: cd frontend && npm run build

  deploy-staging:
    name: Deploy to Staging (Simulated)
    needs: [backend-build-test, frontend-build]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    steps:
      - uses: actions/checkout@v4

      - name: Read deploy configuration
        run: |
          echo "🚀 Starting deployment to Staging Environment..."
          echo "✅ Backend tests passed."
          echo "✅ Frontend build checks passed."
          echo "This is a placeholder for actual deployment scripts such as SSHing into the Linux server, pulling Docker images, and running 'docker compose up -d'."
          echo "Deployment script simulation complete."
